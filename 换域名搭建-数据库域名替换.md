UPDATE `api_apilist` 
SET 
    `apiurl` = REPLACE(`apiurl`, 'https://api.cenguigui.cn', 'https://api-v2.cenguigui.cn'),
    `apirequest` = REPLACE(`apirequest`, 'https://api.cenguigui.cn', 'https://api-v2.cenguigui.cn'),
    `example` = REPLACE(`example`, 'https://api.cenguigui.cn', 'https://api-v2.cenguigui.cn'),
    `examples` = REPLACE(`examples`, 'https://api.cenguigui.cn', 'https://api-v2.cenguigui.cn'),
    `explain` = REPLACE(`explain`, 'https://api.cenguigui.cn', 'https://api-v2.cenguigui.cn'),
    `return` = REPLACE(`return`, 'https://api.cenguigui.cn', 'https://api-v2.cenguigui.cn'),
    `remarks` = REPLACE(`remarks`, 'https://api.cenguigui.cn', 'https://api-v2.cenguigui.cn')
WHERE `apiurl` LIKE '%https://api.cenguigui.cn%' 
   OR `apirequest` LIKE '%https://api.cenguigui.cn%'
   OR `example` LIKE '%https://api.cenguigui.cn%'
   OR `examples` LIKE '%https://api.cenguigui.cn%'
   OR `explain` LIKE '%https://api.cenguigui.cn%'
   OR `return` LIKE '%https://api.cenguigui.cn%'
   OR `remarks` LIKE '%https://api.cenguigui.cn%';
   
   
   
   
   
   
   
   
   
   
   
   
   
比如我想：https://api.cenguigui.cn换成https://api-v2.cenguigui.cn替换即可