# 安全说明

## 支持范围

安全修复以仓库默认分支的最新版本为准。该项目包含较早期的 PHP 代码与第三方前端组件，尚未承诺通过独立安全审计；正式部署前请结合实际环境自行评估。

## 报告安全问题

如果发现可能影响用户数据、认证、文件上传或服务器安全的问题，请不要先在公开 Issue 中披露利用细节。可通过 `cenguigui@qq.com` 联系维护者，并提供：

- 受影响的版本或提交
- 问题位置与复现步骤
- 可能影响
- 建议修复方式（如有）

普通功能问题可直接提交 GitHub Issue。

## 部署者安全清单

- 使用随机且唯一的后台强密码
- 使用最小权限数据库账号
- 启用 HTTPS
- 安装后保留安装锁，并删除或限制 `install/` 的公网访问
- 禁止 Web 访问 `includes/database.php`、日志、备份和版本控制目录
- 仅允许必要目录由 Web 服务用户写入
- 定期更新 PHP、Web 服务器和第三方组件
- 定期备份并验证恢复流程

