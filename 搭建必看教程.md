# API 管理系统搭建与使用说明

    由笒鬼鬼二开美化 程序是22年的产物手搓代码写得不好勿喷
    QQ: 2963246343

## 一、安装步骤

1. 将本项目上传至服务器指定目录。
2. 在浏览器访问 `http://你的域名/install`，根据提示完成安装流程。
3. 安装完成后，后台管理地址为：`http://你的域名/admin`


## (自助安装)
- 或者自己安装，导入install/api.sql到数据库中，编写includes/database.php中的数据库信息即可

## 二、初始登录信息

- **默认用户名**：admin
- **默认密码**：123456

> 安装过程中可自定义用户名和密码，建议安装后及时修改初始密码以保障安全。

## 三、Nginx 配置示例

如需使用 Nginx 作为 Web 服务器，请参考以下配置：

```nginx
location / {
    rewrite ^/index.html$ /index.php;
    rewrite ^/sitemap.xml$ /sitemap.php;
    rewrite ^/start$ /stats_display.php?alias=$1;
    rewrite ^/api/(\w*).html$ /api.php?alias=$1;
    rewrite ^/down-([1-9]+[0-9]*).html$ /down/down.php?id=$1;
    rewrite ^/down.html$ /down/index.php;
}
```

## 四、目录与文件说明

- **API 存放目录**：可自定义，建议统一放置于 `api` 子目录下。
- **资源站地址**：`http://你的域名/down`
- **站点地图**：`http://你的域名/sitemap.xml`
- **接口统计图**：
  - 总览：`http://你的域名/start`
  - 指定接口：`http://你的域名/start?alias=接口别名`
- **API 列表文件**：`apilist.php`
- **API 列表页面**：`apilist.html`（请将其中的网址修改为你自己的域名）

## 五、常见问题

- 安装后如无法访问后台，请检查服务器权限及伪静态（rewrite）配置是否正确。
- 如需更换 API 存放目录，请同步修改相关配置文件。
- 图标怎么换？请看根目录下的 图标配置-1.png，图标配置-2.png，图标配置-3.png
- 为啥后台网站信息配置得其他配置没有看到前端显示，因为只有第一个主题支持，第二个主题支持哦~

---


## 六、统计调用

// 引入统计(Qq_Dg是后台的别名)
// include("../../includes/api_stats.php");
include($_SERVER['DOCUMENT_ROOT'] . '/includes/api_stats.php');
updateApiStatsByAlias('Qq_Dg');
// 统计end
